// Screen 8

import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const PartTwoIntro = () => {
  return (
    <SafeAreaView>
      <Text>PartTwoIntro</Text>
    </SafeAreaView>
  );
};

export default PartTwoIntro