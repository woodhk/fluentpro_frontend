// Screen 11

import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const PartTwoComplete = () => {
  return (
    <SafeAreaView>
      <Text>PartTwoComplete</Text>
    </SafeAreaView>
  );
};

export default PartTwoComplete