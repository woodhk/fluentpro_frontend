// Screen 2

import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const PartOneIntro = () => {
  return (
    <SafeAreaView>
      <Text>PartOneIntro</Text>
    </SafeAreaView>
  );
};

export default PartOneIntro