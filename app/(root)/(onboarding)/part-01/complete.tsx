// Screen 7

import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const PartOneComplete = () => {
  return (
    <SafeAreaView>
      <Text>PartOneComplete</Text>
    </SafeAreaView>
  );
};

export default PartOneComplete