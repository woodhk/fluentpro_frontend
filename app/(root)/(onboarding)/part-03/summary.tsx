// Screen 12

import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const OnboardingSummary = () => {
  return (
    <SafeAreaView>
      <Text>OnboardingSummary</Text>
    </SafeAreaView>
  );
};

export default OnboardingSummary